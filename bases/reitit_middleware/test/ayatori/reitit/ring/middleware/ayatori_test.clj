(ns ayatori.reitit.ring.middleware.ayatori-test
  (:require [clojure.test :as test :refer :all]
            [ayatori.reitit.ring.middleware.ayatori :as ring-middleware]))

(deftest dummy-test
  (is (= 1 1)))
